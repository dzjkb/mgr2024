# @package _global_

defaults:
  - override /callbacks: betawarmup50000
  - override /model: medium_capacity
  - override /dataset: sr48000_3s

model:
  detect_nans: true

train_dataset_path: data/piano/graz_train
val_dataset_path: data/piano/graz_val
log_dir: logs/graz
experiment_name: graz
epochs: 300
batch_size: 2